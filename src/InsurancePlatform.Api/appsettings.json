{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Notifications": {
    "_comment": "When true, a failed OTP gateway delivery does NOT block the OTP flow (the code is still stored and verifiable). Development-only escape hatch - production must keep this false.",
    "AllowOtpDeliveryFailure": false
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;User ID=CHANGE_ME;Password=CHANGE_ME;Database=insurance_platform;Pooling=True;Min Pool Size=2;Max Pool Size=50;Connection Lifetime=180;Connection Reset=True;Connection Timeout=30;CharacterSet=utf8mb4;"
  },
  "ScapiGateway": {
    "_comment": "TODO - replace with the real SCAPI URL/credentials for this environment (local overrides live in appsettings.Development.json, which is gitignored).",
    "Url": "https://scapi.riziki.app/api/Solid/SubmitRequest",
    "ApiUser": "CHANGE_ME_BASE64",
    "ApiPassword": "CHANGE_ME_BASE64",
    "LogoUrl": "/root/scapi/bimadlineportaclient/wwwroot/assets/static/img/bimalogo.png",
    "OffersAttachmentPath": "/root/scapi/offersfiles"
  },
  "QuoteOffersStorage": {
    "_comment": "Shared folder uploaded underwriter-quote documents are written to (and streamed back from). Ubuntu server: /home/insurancePlatform/requestOffers. Override in appsettings.Development.json with a Windows path for local runs.",
    "Root": "/home/insurancePlatform/requestOffers"
  },
  "Mpesa": {
    "_comment": "TODO - replace BusinessShortCode/CallbackUrl with the real Daraja paybill/callback for this environment. CallbackUrl must be a publicly-reachable URL Safaricom's Daraja API can POST to - it is NOT the same as PUT /api/payments/{id}/status (that's OUR endpoint a ChannelService-authenticated caller hits after resolving the callback; the URL below is what SCAPI/Daraja itself calls).",
    "BusinessShortCode": "CHANGE_ME",
    "CallbackUrl": "CHANGE_ME_PUBLIC_URL"
  },
  "GovConnect": {
    "_comment": "TODO - replace with the real GovConnect (KRA PIN checker) URL/credentials. Local overrides live in the gitignored appsettings.Development.json.",
    "Url": "https://api.kra.go.ke",
    "ConsumerKey": "CHANGE_ME",
    "SecretKey": "CHANGE_ME"
  },
  "Jwt": {
    "_comment": "DEV ONLY. Move Key to a proper secret store (user-secrets / environment variable / key vault) before this ever runs anywhere but your machine. Local dev value lives in the gitignored appsettings.Development.json.",
    "Key": "CHANGE_ME_REPLACE_WITH_LONG_RANDOM_SECRET",
    "Issuer": "InsurancePlatform.Api",
    "Audience": "InsurancePlatform.Clients",
    "ExpiryMinutes": 60
  }
}
